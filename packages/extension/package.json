{
  "name": "@curbcut/extension",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "vite build -c vite.config.ts && vite build -c vite.audit.config.ts",
    "package": "npm run build && cd dist && zip -qr ../curbcut-extension.zip . && cd .. && echo \"Wrote packages/extension/curbcut-extension.zip\""
  },
  "dependencies": {
    "@curbcut/core": "*",
    "@curbcut/scan": "*",
    "axe-core": "^4.13.0"
  },
  "devDependencies": {
    "@types/chrome": "^0.0.287",
    "vite": "^6.0.7"
  }
}